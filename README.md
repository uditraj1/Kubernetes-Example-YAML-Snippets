# Kubernetes-Example-YAML-Snippets

This repository is for adding sample code snippets of the basic files needed to setup a Kubernetes cluster with multiple containers, load balancing and multiple pods. 
